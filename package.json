{
  "name": "giftvault-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npm run build",
    "start": "if [ -f server/dist/index.js ]; then node server/dist/index.js; elif [ -f dist/index.js ]; then node dist/index.js; else cd server && npm start; fi"
  }
}
